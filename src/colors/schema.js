/**
 * since neon is combination of
 * dark color, we need to
 * set these value.
 */

// module - schema
let _schema = {
    'primary': "#12102a",
    'secondary': "#1e102a",
};

/** @type {import('tailwindcss').Config} */
module.exports = _schema;

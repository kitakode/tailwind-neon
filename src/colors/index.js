// tailwind - colors
const colors = require("tailwindcss/colors");

// module - colors
let _colors = {
    'neon-blue': colors.cyan[500],
};

/** @type {import('tailwindcss').Config} */
module.exports = _colors
